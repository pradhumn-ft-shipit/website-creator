{
  "name": "@wri/template-trust",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "WRI Trust reference template (PRD §6.1) — serif/navy/charcoal, credentials-heavy, long-form. Renders the shared content schema from @wri/shared.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "lighthouse": "astro build && node scripts/lighthouse.mjs",
    "a11y": "astro build && node scripts/a11y.mjs"
  },
  "dependencies": {
    "@astrojs/tailwind": "6.0.2",
    "@fontsource-variable/libre-franklin": "^5.1.0",
    "@fontsource-variable/newsreader": "^5.1.0",
    "@wri/shared": "file:../_shared",
    "astro": "5.18.2",
    "tailwindcss": "3.4.19"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "axe-core": "^4.10.2",
    "lighthouse": "^12.8.1",
    "puppeteer": "^24.10.0",
    "typescript": "^5.9.3"
  }
}
